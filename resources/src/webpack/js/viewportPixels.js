import jQuery from 'jquery';

jQuery(($) => {
  //var w = document.documentElement.clientWidth;
  //var h = document.documentElement.clientHeight;

  // console.log(w + " ja " + h); This jsut gets the browser width and height
  // debug js


});
